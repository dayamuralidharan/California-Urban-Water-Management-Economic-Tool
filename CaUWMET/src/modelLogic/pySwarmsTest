import sys
import numpy as np
from pyswarms.single.global_best import GlobalBestPSO
from pyswarms.utils.functions import single_obj as fx

#print(fx)

def objectiveFunction(x):
    f = x[0] * 2 #### Why x[0] and not x?
    return f

x_max = [10, 10]
x_min = [1, 1]
bounds = (x_min, x_max)

# Set-up hyperparameters
options = {'c1': 0.5, 'c2': 0.3, 'w':0.9}
optimizer = GlobalBestPSO(n_particles=1, dimensions=1, options=options) #, bounds=bounds) Adding breaks code

cost, pos = optimizer.optimize(objectiveFunction, 1000)

# print("cost:")
# print(cost)
# print("pos:")
# print(pos)